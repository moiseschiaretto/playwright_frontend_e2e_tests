# Opcional — só use se quiser sobrescrever o usuário padrão de demonstração.
# Os usuários públicos do SauceDemo (standard_user, locked_out_user, etc.)
# já funcionam sem precisar preencher nada aqui. Veja tests/support/helpers/credentials.ts
TEST_USERNAME=
TEST_PASSWORD=
